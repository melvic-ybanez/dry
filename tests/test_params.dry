def test_params_binding_order() {
    def bind_vars(a, b, c) {
        assert_equal("first param", a, 1);
        assert_equal("second param", b, 2);
        assert_equal("third param", c, 3);
    }

    bind_vars(1, 2, 3);
}

test_params_binding_order();